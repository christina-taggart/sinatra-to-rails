<%= form_for [@post, @comment] do |f| %>
  Description: <%= f.text_area :description %><br />
  <%= @comment.errors[:description].join("<br>") if @comment.errors[:description] %>
  <%= f.submit %>
<% end %>