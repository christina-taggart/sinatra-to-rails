<h1>Andy-List Categories</h1>
<ul>
  <% @categories.each do |category| %>
    <li><%= link_to category.name, category_posts_path(category) %></li>
  <% end %>
</ul>
<p><%= link_to 'Create New Category', new_category_path %></p>