<ul>
  <% @category.posts.each do |post| %>
    <li>
      <%= post.title %>
      <%= post.content %>
    </li>
  <% end %>
   <%= link_to "delete", category_path(@category), :method => :delete %>

</ul>